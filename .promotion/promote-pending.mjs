import fs from 'node:fs';
import path from 'node:path';
import {execFileSync} from 'node:child_process';

const root=process.cwd();
const read=p=>fs.readFileSync(path.join(root,p),'utf8');
const write=(p,v)=>{const f=path.join(root,p);fs.mkdirSync(path.dirname(f),{recursive:true});fs.writeFileSync(f,v)};
function replaceOnce(text,from,to,label){if(!text.includes(from))throw new Error(`${label} anchor missing`);return text.replace(from,to)}
function replaceRegex(text,re,to,label){if(!re.test(text))throw new Error(`${label} anchor missing`);return text.replace(re,to)}

const VERSION='0.8.45';
const VERSION_CODE=845;

let app=read('app/src/main/assets/app.js');
app=replaceOnce(app,"const ANDROID_CURRENT_VERSION='0.8.44';",`const ANDROID_CURRENT_VERSION='${VERSION}';`,'Android UI version');
app=replaceOnce(app,'const ANDROID_CURRENT_CHANGELOG=[\n',`const ANDROID_CURRENT_CHANGELOG=[\n  'Turns the Google TV startup screen into a cinema-style experience with playful rotating lines instead of technical update, provider or cache messages.',\n  'Makes the launch progress bar much larger and removes foreground percentages/status jargon while preserving the real underlying preparation progress.',\n  'Caps the foreground update-check wait to about 2.8 seconds; the native verified updater continues safely underneath instead of holding the account chooser hostage.',\n`,'current changelog');
app=app.replace('androidLatestManifest={version:ANDROID_CURRENT_VERSION,versionCode:844,','androidLatestManifest={version:ANDROID_CURRENT_VERSION,versionCode:845,');

const funHelpers=`const ANDROID_BOOT_FUN_LINES=[\n  'Grabbing the popcorn…',\n  'Ushers are checking tickets…',\n  'Polishing the big screen…',\n  'Dimming the house lights…',\n  'Finding the best seats…',\n  'Tuning the surround sound…',\n  'Enhancing the cinematic experience…',\n  'Rolling the opening credits…'\n];\nlet androidBootFunIndex=0,androidBootFunTimer=null;\nfunction androidBootFunLine(){return ANDROID_BOOT_FUN_LINES[androidBootFunIndex%ANDROID_BOOT_FUN_LINES.length]}\nfunction startAndroidBootFunTicker(){\n  if(androidBootFunTimer)clearInterval(androidBootFunTimer);androidBootFunIndex=0;\n  const tick=()=>{const el=document.querySelector('#startupBootFunTitle');if(el)el.textContent=androidBootFunLine()};\n  tick();androidBootFunTimer=setInterval(()=>{androidBootFunIndex=(androidBootFunIndex+1)%ANDROID_BOOT_FUN_LINES.length;tick()},1350);\n}\nfunction stopAndroidBootFunTicker(){if(androidBootFunTimer){clearInterval(androidBootFunTimer);androidBootFunTimer=null}}\n\n`;
app=replaceOnce(app,'function startupRefreshPage(){',funHelpers+'function startupRefreshPage(){','cinema boot helpers');
app=replaceRegex(app,/function startupRefreshPage\(\)\{[\s\S]*?\n\}\n(?=function updateStartupRefreshProgress)/,`function startupRefreshPage(){\n  const pct=Math.max(0,Math.min(100,Number(startupRefreshState.progress||0)));\n  if(androidBootSequenceActive)return \`<main class="page restoring-page"><div class="restore-card startup-refresh-card startup-boot-card startup-cinema-card"><img class="startup-swoop-logo" src="./assets/swoop-tv-logo-transparent.png" alt="Swoop TV" /><div class="eyebrow boot-fun-kicker">SWOOP TV PRESENTS</div><h1 class="boot-fun-title" id="startupBootFunTitle">\${esc(androidBootFunLine())}</h1><div class="restore-progress boot-fun-progress"><i><b id="startupRefreshBar" style="width:\${pct}%"></b></i></div><div class="boot-fun-caption">Almost showtime.</div></div></main>\`;\n  return \`<main class="page restoring-page"><div class="restore-card startup-refresh-card startup-boot-card"><img class="startup-swoop-logo" src="./assets/swoop-tv-logo-transparent.png" alt="Swoop TV" /><div class="eyebrow" id="startupRefreshKicker">\${esc(startupRefreshState.kicker||'SWOOP TV')}</div><h1 id="startupRefreshTitle">\${esc(startupRefreshState.title||'Starting Swoop TV…')}</h1><p id="startupRefreshText">\${esc(startupRefreshState.detail||'Getting everything ready.')}</p><div class="restore-progress"><div><span id="startupRefreshCount">\${esc(startupRefreshState.provider||'Preparing…')}</span><strong id="startupRefreshPercent">\${Math.round(pct)}%</strong></div><i><b id="startupRefreshBar" style="width:\${pct}%"></b></i></div><small id="startupRefreshSummary">\${esc(startupRefreshState.summary||'Preparing your Swoop TV experience.')}</small></div></main>\`;\n}\n`,'startup cinema page');

app=replaceRegex(app,/async function runAndroidBootUpdateCheck\(\)\{[\s\S]*?\n\}\n(?=async function bootstrapAndroidPreLogin)/,`async function runAndroidBootUpdateCheck(){\n  updateStartupRefreshProgress({progress:8});\n  let status=androidUpdateStatusSync(),auto=status.automaticUpdates!==false;\n  androidUpdateBridgeCall('checkForUpdate',auto);\n  const started=Date.now();let permissionSeenAt=0;\n  while(Date.now()-started<2800){\n    status=androidUpdateStatusSync();const phase=String(status.phase||'idle'),download=Math.max(0,Math.min(100,Number(status.progress||0)));\n    if(phase==='checking'||phase==='idle'){const elapsed=Math.min(1,(Date.now()-started)/2200);updateStartupRefreshProgress({progress:8+elapsed*12});}\n    else if(phase==='downloading')updateStartupRefreshProgress({progress:18+(download/100)*14});\n    else if(phase==='installing'||phase==='approval_required')updateStartupRefreshProgress({progress:32});\n    else if(phase==='permission_required'){if(!permissionSeenAt)permissionSeenAt=Date.now();updateStartupRefreshProgress({progress:20});if(status.canInstallPackages){permissionSeenAt=0;await androidBootWait(120);continue}if(Date.now()-permissionSeenAt>900)break;}\n    else if(phase==='available'){updateStartupRefreshProgress({progress:30});break;}\n    else if(phase==='up_to_date'||phase==='error')break;\n    else break;\n    await androidBootWait(120);\n  }\n  updateStartupRefreshProgress({progress:34});\n}\n`,'short non-blocking update check');
app=replaceOnce(app,'  render();\n  setTimeout(()=>{refreshPerformancePackInfo().catch(()=>null);prepareStarmeterBeforeLogin().catch(()=>false)},0);','  render();startAndroidBootFunTicker();\n  setTimeout(()=>{refreshPerformancePackInfo().catch(()=>null);prepareStarmeterBeforeLogin().catch(()=>false)},0);','boot fun ticker start');
app=replaceOnce(app,'  androidBootSequenceActive=false;startupRefreshActive=false;profilePickerOpen=true;state.page=\'home\';render();','  stopAndroidBootFunTicker();androidBootSequenceActive=false;startupRefreshActive=false;profilePickerOpen=true;state.page=\'home\';render();','boot fun ticker stop');
write('app/src/main/assets/app.js',app);

let css=read('app/src/main/assets/styles.css');
css+=`\n/* v0.8.45 — cinema-style startup presentation. */\nhtml.android-tv .startup-cinema-card{min-width:min(900px,80vw);padding:48px 64px 56px;text-align:center}\nhtml.android-tv .startup-cinema-card .startup-swoop-logo{margin-bottom:18px}\nhtml.android-tv .startup-cinema-card .boot-fun-kicker{font-size:12px;letter-spacing:.3em;color:var(--accent-2)}\nhtml.android-tv .startup-cinema-card .boot-fun-title{font-size:clamp(36px,4.2vw,60px);line-height:1.02;min-height:1.08em;margin:14px 0 0;letter-spacing:-.045em}\nhtml.android-tv .startup-cinema-card .boot-fun-progress{width:100%;max-width:760px;margin:34px auto 0}\nhtml.android-tv .startup-cinema-card .boot-fun-progress>i{display:block;height:24px!important;border-radius:999px;background:rgba(255,255,255,.1);box-shadow:inset 0 0 0 1px rgba(255,255,255,.1),0 10px 34px rgba(0,0,0,.28)}\nhtml.android-tv .startup-cinema-card .boot-fun-progress>i>b{transition:width .34s ease}\nhtml.android-tv .startup-cinema-card .boot-fun-caption{margin-top:14px;font-size:13px;font-weight:800;letter-spacing:.08em;color:#aeb2bd}\n`;
write('app/src/main/assets/styles.css',css);

let gradle=read('app/build.gradle');
gradle=replaceOnce(gradle,'versionCode 844',`versionCode ${VERSION_CODE}`,'Gradle versionCode');
gradle=replaceOnce(gradle,"versionName '0.8.44'",`versionName '${VERSION}'`,'Gradle versionName');
write('app/build.gradle',gradle);

let activity=read('app/src/main/java/tv/swoop/player/MainActivity.java');
activity=replaceOnce(activity,'SwoopTV/0.8.44 AndroidTV',`SwoopTV/${VERSION} AndroidTV`,'Android user agent');
write('app/src/main/java/tv/swoop/player/MainActivity.java',activity);

let tests=read('tests/tv-ui-runtime-smoke.mjs');
tests=replaceOnce(tests,"if (!appSource.includes('async function bootstrapAndroidPreLogin()') || !appSource.includes(\"title:'Checking for updates…'\") || !appSource.includes(\"title:'Loading your library…'\")) throw new Error('Branded pre-login update/library boot pipeline missing');","if (!appSource.includes('async function bootstrapAndroidPreLogin()') || !appSource.includes('ANDROID_BOOT_FUN_LINES') || !appSource.includes('startupBootFunTitle') || !appSource.includes('while(Date.now()-started<2800)')) throw new Error('Cinema-style non-blocking pre-login boot pipeline missing');",'boot regression');
tests=replaceOnce(tests,"if (!appSource.includes(\"const ANDROID_CURRENT_VERSION='0.8.44';\")) throw new Error('Current Android UI version marker missing');",`if (!appSource.includes("const ANDROID_CURRENT_VERSION='${VERSION}';")) throw new Error('Current Android UI version marker missing');`,'test version marker');
tests=replaceOnce(tests,"if (!cssSource.includes('width:min(440px,54vw)')) throw new Error('Large Swoop TV boot logo missing');","if (!cssSource.includes('width:min(440px,54vw)') || !cssSource.includes('height:24px!important') || !cssSource.includes('.startup-cinema-card')) throw new Error('Large Swoop TV cinema boot presentation missing');",'boot CSS regression');
write('tests/tv-ui-runtime-smoke.mjs',tests);

let notes=read('RELEASE_NOTES.md');
notes=`## v${VERSION} — Cinema Startup + Non-Blocking Update Check\n\n- Replaces technical startup status copy with rotating cinema-style lines such as **Grabbing the popcorn**, **Ushers are checking tickets**, **Dimming the house lights**, and **Enhancing the cinematic experience**.\n- Keeps one clean Swoop TV logo/progress experience and hides GitHub, update-download, provider, cache, library-count and percentage jargon from the Google TV boot screen.\n- Enlarges the Google TV startup progress bar to a prominent 24px theatre-style track while preserving the real monotonic preparation progress underneath.\n- Caps the foreground update-check wait at roughly **2.8 seconds** so a slow GitHub response no longer holds the user on the boot screen; the verified native updater continues safely underneath.\n- Preserves the automatic one-time Android install-permission prompt and in-place self-update path when an update is found.\n- Preserves v0.8.44 shared-household/private-provider account isolation and existing user data migration.\n- Android versionName/versionCode are **${VERSION}/${VERSION_CODE}**.\n\n${notes}`;
write('RELEASE_NOTES.md',notes);

execFileSync('git',['config','user.name','github-actions[bot]']);
execFileSync('git',['config','user.email','41898282+github-actions[bot]@users.noreply.github.com']);
fs.rmSync(path.join(root,'.promotion/promote-pending.mjs'),{force:true});
execFileSync('git',['add','app/src/main/assets/app.js','app/src/main/assets/styles.css','app/build.gradle','app/src/main/java/tv/swoop/player/MainActivity.java','tests/tv-ui-runtime-smoke.mjs','RELEASE_NOTES.md','.promotion/promote-pending.mjs']);
execFileSync('git',['commit','-m',`Promote v${VERSION} cinema startup and non-blocking update check [skip ci]`],{stdio:'inherit'});
execFileSync('git',['push','origin','HEAD:main'],{stdio:'inherit'});
console.log(`Promoted v${VERSION} cinema-style startup and non-blocking updater.`);
